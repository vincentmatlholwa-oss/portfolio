services:
  - type: web
    name: portfolio
    runtime: static
    plan: free
    buildCommand: .
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
